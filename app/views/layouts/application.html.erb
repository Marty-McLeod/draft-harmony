<!DOCTYPE html>
<html>
  <head>
    <title>Draft Harmony</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <!-- Add custom favicon using helper -->
    <%= favicon_link_tag 'favicon-draft-harmony.png', type: 'image/png' %>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <!-- Bootstrap is added via the stylesheet helper & application.scss @import -->
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v6.1.2/css/all.css">
  </head>

  <body>
    <%= link_to "Skip to main content", "#main-content", class: "visually-hidden-focusable" %>
    <header>
      <%= render "shared/navbar" %> <!-- 👈 Add this for navbar -->
    </header>
    
    <main id="main-content">
      <%= render "shared/flashes" %>
      <%= yield %>
    </main>
  </body>

</html>
